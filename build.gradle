plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.9.0'
    id 'com.github.johnrengelman.shadow' version '8.0.0'
    id 'maven-publish'
}

group = 'dev.sora.relay'
version = '1.4.0'

repositories {
    mavenCentral()
    maven { url 'https://jitpack.io' }
    maven { url 'https://repo.opencollab.dev/maven-releases/' }
    maven { url 'https://repo.opencollab.dev/maven-snapshots/' }
    maven { url 'https://libraries.minecraft.net/' }
    // CloudburstMC için alternatif repo
    maven { url 'https://repo.cloudburstmc.org/releases/' }
}

configurations {
    embed
    implementation.extendsFrom(embed)
}

dependencies {
    // Kotlin
    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-core:1.7.2"

    // Minecraft/Bedrock - Güncellenmiş bağımlılık
    embed('org.cloudburstmc.protocol:bedrock-connection:3.0.0.Beta3') {
        exclude group: 'com.nukkitx', module: 'natives'
    }

    // Auth - Çalışan alternatif
    implementation 'com.github.JellySquid3:msft-auth:1.2.0'

    // LevelDB
    implementation 'org.iq80.leveldb:leveldb:0.12'

    // Utilities
    implementation 'com.google.code.gson:gson:2.10.1'
    implementation('com.squareup.okhttp3:okhttp:4.11.0')
    
    // Security
    implementation 'commons-codec:commons-codec:1.15'
}

kotlin {
    jvmToolchain(17)
}

tasks.named('shadowJar') {
    archiveClassifier = ''
    configurations = [project.configurations.embed]
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    
    manifest {
        attributes 'Main-Class': 'dev.sora.relay.MainKt'
    }
    
    relocate 'com.google', 'shadow.com.google'
    relocate 'okhttp3', 'shadow.okhttp3'
    relocate 'okio', 'shadow.okio'
}

publishing {
    publications {
        shadow(MavenPublication) { 
            from components.java
            artifactId = 'relay'
        }
    }
}

plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.9.0'
    id 'com.github.johnrengelman.shadow' version '8.0.0'
    id 'maven-publish'
}

group = 'dev.sora.relay'
version = '1.4.0'

repositories {
    mavenCentral()
    maven { url 'https://jitpack.io' }  // GitHub tabanlı bağımlılıklar için
    maven { url 'https://repo.papermc.io/repository/maven-public/' }
    maven { url 'https://libraries.minecraft.net/' }
}

configurations {
    embed
    implementation.extendsFrom(embed)
}

dependencies {
    // Kotlin kütüphaneleri
    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-core:1.7.2"

    // Minecraft/Bedrock bağlantısı için alternatif
    embed('com.github.CloudburstMC.Protocol:bedrock-connection:3.0.0.Beta1') {
        exclude group: 'com.nukkitx', module: 'natives'
    }

    // Microsoft Auth için alternatif
    embed 'com.github.MCMrARM:msft-auth-kotlin:1.0.0'

    // LevelDB için alternatif
    embed 'org.iq80.leveldb:leveldb:0.12'

    // Diğer gerekli kütüphaneler
    embed 'com.google.code.gson:gson:2.10.1'
    embed('com.squareup.okhttp3:okhttp:4.11.0') {
        exclude group: 'org.jetbrains.kotlin'
    }
    
    // Ek güvenlik kütüphaneleri
    embed 'commons-codec:commons-codec:1.15'
}

kotlin {
    jvmToolchain(17)
}

shadowJar {
    archiveClassifier.set('')
    configurations = [project.configurations.embed]
    duplicatesStrategy DuplicatesStrategy.EXCLUDE
    
    manifest {
        attributes 'Main-Class': 'dev.sora.relay.MainKt' // Ana sınıfınızı güncel
